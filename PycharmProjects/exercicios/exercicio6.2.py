base_str = input('informe a base do retangulo: ')
base = float(base_str)

altura_str = input('informe a base do retangulo: ')
altura = float(altura_str)

AreaRetangulo = (base*altura)

print('A area do retangulo é : ', AreaRetangulo)